{
  "name": "vector-space-trader",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs",
    "test:browser": "node scripts/smoke.mjs --browser"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.4"
  }
}
